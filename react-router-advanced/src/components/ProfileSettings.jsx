/* eslint-disable no-unused-vars */
import React from "react";

const ProfileSettings = () => {
  return (
    <div>
      <h2>Profile Settings</h2>
      <p>This is where the user profile settings are managed.</p>
    </div>
  );
};

export default ProfileSettings;
